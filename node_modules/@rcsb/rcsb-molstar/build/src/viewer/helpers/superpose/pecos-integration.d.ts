import { Mat4 } from 'molstar/lib/mol-math/linear-algebra';
export declare type Residue = {
    label_asym_id: string;
    struct_oper_id?: string;
    label_seq_id: number;
};
export declare type MotifSelection = {
    entry_id: string;
    residue_ids: Residue[];
};
export declare type MotifAlignmentRequest = {
    query: {
        entry_id: string;
        residue_ids: Residue[];
    };
    hits: ({
        id: string;
        assembly_id: string;
    } & MotifSelection)[];
};
export declare function alignMotifs(query: MotifSelection, hit: MotifSelection): Promise<{
    rmsd: number;
    matrix: Mat4;
}>;
