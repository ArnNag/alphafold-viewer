import { PluginContext } from 'molstar/lib/mol-plugin/context';
export declare function encodeStructureData(plugin: PluginContext): {
    [k: string]: Uint8Array;
};
export declare function downloadAsZipFile(plugin: PluginContext, content: {
    [k: string]: Uint8Array;
}): Promise<void>;
